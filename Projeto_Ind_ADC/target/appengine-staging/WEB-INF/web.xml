<?xml version="1.0" encoding="utf-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd" version="3.1">

	<display-name>My First Web Application</display-name>

	<!-- Runs on application startup: creates the root ADMIN account from
	     configuration if none exists yet (see AdminSeeder / appengine-web.xml). -->
	<listener>
		<listener-class>pt.unl.fct.di.adc.firstwebapp.Utilities.AdminSeeder</listener-class>
	</listener>

	<welcome-file-list>
		<welcome-file>index.html</welcome-file>
	</welcome-file-list>

	<servlet>
		<servlet-name>MainServices</servlet-name>
		<servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
		<init-param>
			<param-name>jakarta.ws.rs.Application</param-name>
			<param-value>pt.unl.fct.di.adc.firstwebapp.ApplicationConfig</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>MainServices</servlet-name>
		<url-pattern>/rest/*</url-pattern>
	</servlet-mapping>

	<!-- SPA fallback: client-side routes (e.g. /profile, /maps) have no matching
	     static file, so serve the React entry point instead of a 404. -->
	<error-page>
		<error-code>404</error-code>
		<location>/index.html</location>
	</error-page>

</web-app>
